{
  "name": "wanbinyu-harness-toolbox",
  "version": "0.4.1",
  "private": true,
  "description": "Independent project index and verification checks for DeepSeek Harness plugins and tools",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "verify": "npm run verify:catalog && npm run site:build && npm run site:test",
    "verify:catalog": "node scripts/verify-catalog.mjs",
    "site:build": "node scripts/build-site.mjs",
    "site:test": "node --test scripts/site.test.mjs"
  }
}
